services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: abax
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD requerido}
      POSTGRES_DB: abax
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U abax"]
      interval: 5s
    restart: unless-stopped

  authentik-postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: abax
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD requerido}
      POSTGRES_DB: authentik
    volumes:
      - authpgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U abax"]
      interval: 5s
    restart: unless-stopped

  authentik-redis:
    image: redis:7-alpine
    restart: unless-stopped

  authentik-server:
    image: ghcr.io/goauthentik/server:latest
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:?AUTHENTIK_SECRET_KEY requerido}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: abax
      AUTHENTIK_POSTGRESQL__PASSWORD: ${DB_PASSWORD:?DB_PASSWORD requerido}
      AUTHENTIK_REDIS__HOST: authentik-redis
    volumes:
      - media:/media
      - custom-templates:/templates
    ports:
      - "9000:9000"
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_started
    restart: unless-stopped

  authentik-worker:
    image: ghcr.io/goauthentik/server:latest
    command: worker
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:?AUTHENTIK_SECRET_KEY requerido}
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_POSTGRESQL__HOST: authentik-postgres
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: abax
      AUTHENTIK_POSTGRESQL__PASSWORD: ${DB_PASSWORD:?DB_PASSWORD requerido}
      AUTHENTIK_REDIS__HOST: authentik-redis
    volumes:
      - media:/media
    depends_on:
      authentik-postgres:
        condition: service_healthy
      authentik-redis:
        condition: service_started
    restart: unless-stopped

  abax-app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    environment:
      DATABASE_URL: postgresql://abax:${DB_PASSWORD:?DB_PASSWORD requerido}@postgres:5432/abax
      AUTHENTIK_ISSUER: ${AUTHENTIK_ISSUER:-http://localhost:9000/application/o/abax-gantt/}
      AUTHENTIK_CLIENT_ID: ${AUTHENTIK_CLIENT_ID:-abax-gantt-spa}
      AUTHENTIK_JWKS_URL: ${AUTHENTIK_JWKS_URL:-http://authentik-server:9000/application/o/abax-gantt/jwks/}
      STORAGE_PATH: /app/data/attachments
      ADMIN_GROUP: ${ADMIN_GROUP:-abax-admins}
      AUTH_MODE: bundled
      PUBLIC_AUTHENTIK_AUTHORITY: ${PUBLIC_AUTHENTIK_AUTHORITY:-http://localhost:9000/application/o/abax-gantt/}
      PUBLIC_AUTHENTIK_CLIENT_ID: ${PUBLIC_AUTHENTIK_CLIENT_ID:-${AUTHENTIK_CLIENT_ID:-abax-gantt-spa}}
      PUBLIC_AUTHENTIK_REDIRECT_URI: ${PUBLIC_AUTHENTIK_REDIRECT_URI:-http://localhost:8000/auth/callback}
      PUBLIC_AUTHENTIK_POST_LOGOUT_REDIRECT_URI: ${PUBLIC_AUTHENTIK_POST_LOGOUT_REDIRECT_URI:-http://localhost:8000/login}
      PUBLIC_BASE_PATH: ${PUBLIC_BASE_PATH:-/}
      PUBLIC_API_BASE_URL: ${PUBLIC_API_BASE_URL:-}
    ports:
      - "8000:8000"
    volumes:
      - attachments:/app/data/attachments
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
  authpgdata:
  media:
  custom-templates:
  attachments:
